export enum PurchaseSteps {
    email = 'email',
    age = 'age',
    name = 'name',
    summary = 'summary'
}

export type PurchaseStepsType = {
    email?: string,
    age?: string,
    name?: string,
    surname?: string
}